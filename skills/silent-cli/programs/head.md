# head

**Platforms:** Multi-platform  
**Category:** Text Processing

## Command-Line Flags

- `-q` or `--quiet`: No filename headers (suppress headers in multi-file output)

## Recommended Unattended Usage

```bash
#!/bin/bash

# Quiet multi-file output (no headers between files)
head -q -n 10 file1 file2
```
