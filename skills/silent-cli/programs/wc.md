# wc

**Platforms:** Multi-platform  
**Category:** Text Statistics

## Recommended Unattended Usage

```bash
# Count lines from stdin (pure non-interactive usage)
cat file | wc -l
grep pattern file | wc -l
```
